#git commands
git init (new local repo).
git fetch <Remote-repo-name> home. (home is name of branch).
git branch new-branch (new branch).
git push <Remote-repo-name> home. (push to remote branch home).
Note : home branch must be created in local repo.
